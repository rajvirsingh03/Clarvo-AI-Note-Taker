import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Sessions' }

// TODO: Paginated session list with filters
export default function SessionsPage() {
  return (
    <div>
      <h1>Sessions</h1>
    </div>
  )
}
