import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Sign In' }

// TODO: Implement Google OAuth sign-in page with Supabase signInWithOAuth
export default function LoginPage() {
  return (
    <main>
      <h1>Sign in to Clarvo AI</h1>
      {/* Google OAuth button */}
    </main>
  )
}
